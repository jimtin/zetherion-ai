name: Revert Failed Main Deploy (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Rollback Moved to Zetherion
    runs-on: ubuntu-latest
    steps:
      - name: Summarize rollback ownership
        shell: bash
        run: |
          {
            echo "### Rollback Ownership"
            echo ""
            echo "Rollback classification and release blocking are now owned by Zetherion receipts and incidents."
            echo "Do not rely on GitHub workflow-triggered auto-revert for production deploy control."
          } >> "$GITHUB_STEP_SUMMARY"
