# Development overrides - use with: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  zetherion_ai:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      - ENVIRONMENT=development
    volumes:
      - ./src:/app/src:ro
      - ./data:/app/data
    command: ["python", "-m", "watchfiles", "python -m zetherion_ai", "./src"]
