# Core dependencies - pinned versions for reproducible builds
# Last updated: 2026-02-05
discord.py==2.7.1
audioop-lts>=0.2.1;python_version>='3.13'  # Required for discord.py on Python 3.13+ (audioop removed in 3.13)
qdrant-client==1.17.1
google-genai==1.73.1
anthropic==0.96.0
cryptography==46.0.7  # AES-256-GCM encryption for Phase 5A (CVE-2026-26007 fix)
openai==2.32.0
pydantic==2.13.3  # Updated for Python 3.14 support (pydantic-core wheels)
pydantic-settings==2.14.0  # Compatible with pydantic 2.12
structlog==25.5.0
httpx==0.28.1
cachetools==7.0.6  # TTL cache for profile system (Phase 5C)
aiohttp==3.13.5  # Skills service REST API (Phase 5D)
asyncpg==0.31.0  # PostgreSQL async driver for RBAC and settings
bcrypt==5.0.0  # API key hashing for telemetry receiver (Phase 10C)
PyJWT==2.12.1  # JWT session tokens for multi-tenant API
defusedxml==0.7.1  # Safe XML parsing for DOCX ingestion
